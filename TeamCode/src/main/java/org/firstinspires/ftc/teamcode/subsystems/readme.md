# Package Overvew
This package is for the classes and functions to drive the isolated systems.
___
# Formating
- The format of the classes should follow the rules bellow.
    - The class should have the following ***public*** functions with optional inputs and the output does not yet have a standard
      - A "main" function which is for
      - An optional "update" function for non-blocking operation which can have inputs about the current related information and outputs relaying information